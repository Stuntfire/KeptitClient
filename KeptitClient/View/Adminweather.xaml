<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KeptitClient.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModels="using:KeptitClient.ViewModels"
    x:Class="KeptitClient.View.Adminwhether"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


        <Grid.DataContext>
            <ViewModels:GreenkeeperViewModel/>
        </Grid.DataContext>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Left" Margin="119,110,0,0" TextWrapping="Wrap" Text="{Binding Temp, Mode=TwoWay}" VerticalAlignment="Top" Width="44"/>
        <Image x:Name="image" HorizontalAlignment="Left" Height="55" VerticalAlignment="Top" Width="55" Source="{Binding Icon, Mode=TwoWay}" Margin="236,62,0,0" />
          
    </Grid>
</Page>
